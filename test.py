# defining adapters - doesn't work
# adapter_config = BnConfig(
#     mh_adapter=True,
#     output_adapter=False,
#     reduction_factor=16,
#     non_linearity="relu",
# )

# model.add_adapter("base_adapter", config=adapter_config)
# model.train_adapter("base_adapter")